package stickyWallet.utils

object Constants {
    const val consolePrefix = "§2[StickyWallet] §f"
    const val errorPrefix = "§2[StickyWallet Error] §f"

    object SettingsPaths {
        const val StorageDatabase = "storage.database"
        const val StorageTablePrefix = "storage.tablePrefix"
        const val StorageHost = "storage.host"
        const val StoragePort = "storage.port"
        const val StorageUsername = "storage.username"
        const val StoragePassword = "storage.password"
        const val StorageCurrencyTypes = "storage.currencyTypes"

        const val CheckMaterial = "check.material"
        const val CheckName = "check.name"
        const val CheckLore = "check.lore"
        const val CheckConsoleName = "check.consoleName"
        const val CheckConsoleLore = "check.consoleLore"
        const val CheckNoCrafting = "check.noCraftingWithChecksError"

        const val DebugLogs = "debug.logs"
        const val DebugTransactions = "debug.transactionLog"

        const val VaultIntegration = "integrations.vault"
    }

    object TranslationKeys
}
